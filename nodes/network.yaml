tosca_definitions_version: tosca_simple_yaml_1_3

imports:
- contract.yaml

node_types:
  katena.nodes.network: # network can be any type of blockchain (e.g. Eth, L2, Ganache)
    derived_from: katena.nodes.Root
    properties:
      address:
        description: Host IP and port
        type: string
        required: true
    capabilities:
      host:
        type: tosca.capabilities.Container
        valid_source_types: [katena.nodes.contract] # network can host only smart contracts
  katena.nodes.network.ganache: # set default value for ganache
    derived_from: katena.nodes.network
    properties:
      address:
        description: Host IP and port
        type: string
        required: true
        default: 'localhost:8545'
# TODO: add more networks
